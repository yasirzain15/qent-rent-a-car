import 'package:flutter/material.dart';

class AppColors {
  // ================= PRIMARY BRAND COLORS =================
  static const Color primary = Color(0xFF454545); // Main brand color
  static const Color secondary = Color(0xFFEDEDED); // Light UI surfaces
  static const Color tertiary = Color(0xFF3B82F6); // Accent / Highlight (Blue)

  // ================= TEXT COLORS =================
  static const Color textPrimary = Color(0xFF000000); // Main text
  static const Color textSecondary = Color(0xFF7F7F7F); // Subtitle / muted text

  // ================= UI UTILITY COLORS =================
  static const Color stroke = Color(0xFFD7D7D7); // Borders / outlines
  static const Color shade = Color(0xFFFFFFFF); // White surfaces
  static const Color button = Color(0xFF21292B); // Button background
  static const Color icon = Color(0xFF767676); // Icon color
  static const Color background = Color(0xFFF8F8F8); // Screen background

  // ================= STATUS COLORS =================
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFDC2626);
  static const Color info = Color(0xFF0EA5E9);
}
